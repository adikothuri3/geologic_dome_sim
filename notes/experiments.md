---
title: Experiment log
updated: 2026-08-06
status: current
---

# Experiment log

Every training or reconstruction run gets a row — automatically, with the commit hash, at the time of the run. **Rows are never deleted**: failed runs with honest takeaways are the point. Keep it a table; anything longer than a takeaway sentence belongs in the weekly lab notebook (`lab-notebook/`, outside this vault).

Columns: `run_id` (date + short slug), `commit` (short hash of the code that ran), `config` (env/model + the settings that mattered), `n_envs` (parallel envs, or `—` for recon runs), `metrics` (the numbers — see the evaluation harness in [[pipeline]]), `takeaway` (one sentence).

| run_id | commit | config | n_envs | metrics | takeaway |
| --- | --- | --- | --- | --- | --- |
| 2026-08-03-g1-joystick-smoke | 078625b-dirty | G1JoystickFlatTerrain, PPO, bs=8, nmb=32, steps=2,000,000, seed=0 | 256 | final eval reward nan, best nan, 0 min — FAILED | AttributeError: jax.device_put_replicated is deprecated; use jax.device_put instead. See https://docs.jax.dev/en/latest/migrate_pmap.html#drop-in-replacements for a drop-in replacement. |
| 2026-08-03-g1-joystick-smoke | 078625b-dirty | G1JoystickFlatTerrain, PPO, bs=8, nmb=32, steps=2,000,000, seed=0 | 256 | final eval reward -3.07, best -3.07, 3 min | smoke test only, not a usable policy |
| 2026-08-04-g1-joystick | 1aa7006 | G1JoystickFlatTerrain, PPO, bs=64, nmb=32, steps=100,000,000, seed=0 | 2048 | final eval reward 2.96, best 4.53, 27 min | baseline at 2048 envs; reward 3.0 |
| 2026-08-04-g1-joystick-full-collision-smoke | 1aa7006-dirty | G1JoystickFlatTerrain, **full-collision**, PPO, bs=8, nmb=32, njmax=384, steps=2,000,000, seed=0 | 256 | final eval reward -4.02, best -4.02, 3 min | smoke test only, not a usable policy |
| 2026-08-04-g1-joystick-full-collision | c74499d | G1JoystickFlatTerrain, **full-collision**, PPO, bs=64, nmb=32, njmax=384, steps=200,000,000, seed=0 | 2048 | final eval reward 1.58, best 6.18, 59 min | full-collision at 2048 envs; reward 1.6 |
| 2026-08-04-g1-joystick-full-collision-smoke | c74499d-dirty | G1JoystickFlatTerrain, **full-collision**, PPO, bs=8, nmb=32, njmax=384, steps=2,000,000, seed=0 | 256 | final eval reward -2.97, best -2.97, 3 min — FAILED | ValueError: Destination /mnt/c/Users/Aditya/VSCode/GeologicDome/runs/2026-08-04-g1-joystick-full-collision-smoke/params already exists. |
| 2026-08-04-g1-joystick-full-collision-smoke | c74499d-dirty | G1JoystickFlatTerrain, **full-collision**, PPO, bs=8, nmb=32, njmax=384, steps=2,000,000, seed=0 | 256 | final eval reward -2.94, best -2.94, 3 min | smoke test only, not a usable policy |
| 2026-08-04-g1-joystick-full-collision-100M | 028000f | G1JoystickFlatTerrain, **full-collision**, PPO, bs=64, nmb=32, njmax=384, steps=100,000,000, seed=0 | 2048 | final eval reward 2.65, best 4.74, 32 min | full-collision at 2048 envs; reward 2.6 |
| 2026-08-04-g1-joystick-full-collision-100M-feet_air_time4 | e79850c | G1JoystickFlatTerrain, **full-collision**, PPO, bs=64, nmb=32, njmax=384, steps=100,000,000, seed=0, **feet_air_time 2→4** | 2048 | final eval reward 2.17, best 3.05, 32 min | full-collision at 2048 envs; reward 2.2 |
| 2026-08-04-g1-joystick-full-collision-100M-action_rate-0.2-energy-0.0001-feet_clearance-0.5-torques-0.0002 | 766cdf0 | G1JoystickFlatTerrain, **full-collision**, PPO, bs=64, nmb=32, njmax=384, steps=100,000,000, seed=0, **action_rate 0→-0.2**, **energy 0→-0.0001**, **feet_clearance 0→-0.5**, **torques 0→-0.0002** | 2048 | final eval reward -1.68, best -0.94, 32 min | full-collision at 2048 envs; reward -1.7 |
| 2026-08-04-roommap-smoke-portrait | 766cdf0-dirty | LingBot-Map base, room_map.MOV 15 s, **portrait pad→518×518**, windowed ws=16 kfi=4, 60 frames @10 fps, sdpa, bf16 | — | 18.9 s, 3.17 fps, peak VRAM 6.93 GB, 409k pts, 43% of each frame masked as padding | portrait padding burns 43% of the token budget on white pixels |
| 2026-08-04-roommap-smoke-landscape | 766cdf0-dirty | LingBot-Map base, same 15 s clip **rotated CW→518×294 crop**, windowed ws=16 kfi=4, 60 frames | — | 14.7 s, 4.07 fps, peak VRAM **5.15 GB**, 451k pts | rotating portrait→landscape beat padding on every axis: −1.8 GB VRAM, +28% fps, +10% points |
| 2026-08-04-roommap-full-10fps | 766cdf0-dirty | LingBot-Map base, 1320 frames @10 fps, 518×518 portrait, windowed ws=24 | — | **FAILED** — OOM-killed at ~7 GB RSS | predictions accumulate on CPU in fp32 (~7.1 GB vs 7.7 GB WSL RAM); added a preflight RAM estimate that now aborts in 5 s instead of 20 min |
| 2026-08-04-roommap-full-w13 | 766cdf0-dirty | LingBot-Map base, 660 frames @5 fps landscape, windowed ws=16 kfi=6 overlap_kf=4 | — | 193.6 s, 3.41 fps, peak VRAM 5.19 GB, 4.77M pts, **13 windows**, traj length 88.6 vs extent 2.60 (34×) | cross-window alignment failed; pose jump of 2.14 at frame 164 (median step 0.071) |
| 2026-08-04-roommap-full-w24 | 766cdf0-dirty | as above but ws=24, overlap_kf=8, kvsw=32 | — | 211.3 s, 3.12 fps, peak VRAM 6.21 GB, 4.51M pts, **9 windows**, traj length 109.6 vs extent 2.88 (38×) | tripling the overlap did **not** fix stitching — the failure is scale estimation on low-texture walls, not overlap size |
| 2026-08-04-roommap-w32 | 766cdf0-dirty | as above but ws=32, overlap_kf=12 | — | **FAILED** — clean `torch.OutOfMemoryError` at the 6.8 GB cap | window_size is the VRAM driver; 24 keyframes is this card's ceiling at 518×294 |
| 2026-08-04-roommap-stream | 766cdf0-dirty | LingBot-Map base, 660 frames, **streaming** mode, kfi=3 (220 keyframes) | — | **ABANDONED** — stalled at 33/660, GPU pinned 100% at 7858/8188 MiB with 2 GB swap consumed | ~~streaming not viable on 8 GB~~ **wrong conclusion — our bug, see 2026-08-05 rows**: `reconstruct.py` moved the whole image tensor to the GPU in streaming mode |
| 2026-08-04-roommap-single-window | 766cdf0-dirty | LingBot-Map base, 124 frames (25 s) landscape, windowed ws=24 kfi=6 → **exactly 1 window**, conf p55 | — | 26.4 s, 4.70 fps, peak VRAM 6.21 GB, **921k pts**, 1 window, traj 6.75 vs extent 2.45 (**2.8×**), no pose jump | the usable Phase 3 artifact: coherent room, smooth trajectory. Single-window is the only trustworthy config on this box |
| 2026-08-05-roommap-temporal-sweep | 766cdf0-dirty | windowed ws=24 kfi=6, 124 frames each, **stride 2/3/4/6/11** = 0.2/0.3/0.4/0.6/1.1 s between frames | — | ratio 2.8 / 8.6 / 9.7 / 18.7 / 17.7 | **≥5 fps is required.** The cliff between 0.2 s and 0.3 s frame spacing is sharp; sampling a long clip sparsely to fit one window does not work |
| 2026-08-05-roommap-ws32 | 766cdf0-dirty | windowed **ws=32** (172 frames, 34 s), kvsw=12 then 32 | — | 5.39 / 7.22 GB, ratio 7.4 / 7.2 | frames-per-window matters independently of frame spacing: 172 frames degrades even in one window with a full cache |
| 2026-08-05-roommap-lowres | 766cdf0-dirty | `--image_size 336` to buy a bigger window | — | **FAILED** — `size mismatch for aggregator.patch_embed.pos_embed: [1,1370,1024] vs [1,577,1024]` | resolution is baked into the checkpoint's position embeddings; 518 is not a tunable |
| 2026-08-05-roommap-stream-fixed | 766cdf0-dirty | **streaming**, 660 frames (132 s), kfi=6, **kvsw=16**, inputs left on CPU | — | 147.7 s, 4.47 fps, peak VRAM **5.63 GB**, 4.89M pts, ran to completion | **corrects the Aug 4 streaming row.** Memory is flat over the whole clip — streaming was never the problem; moving the image tensor to the GPU was |
| 2026-08-05-roommap-stream-sweep | 766cdf0-dirty | streaming, 660 frames, (kvsw 24, kfi 6) and (kvsw 16, kfi 12) | — | 6.55 GB → ratio 34.1; 5.57 GB → ratio 45.8 | a bigger cache helps but cannot be afforded past kvsw 24 (32 OOMs); longer horizon via sparser keyframes is worse than more slots |
| 2026-08-05-roommap-stream-segments | 766cdf0-dirty | streaming, kvsw=24 kfi=6, first **25 s** then first **49 s** | — | 5.99 GB ratio **2.76** (921,100 pts — byte-identical to the windowed run); 6.47 GB ratio 11.8 | **drift is a function of clip length, not mode.** Both modes agree exactly at 25 s and both fail past it |
| 2026-08-05-landscape-a | 766cdf0-dirty | LingBot-Map base, `room_map_landscape.MOV` (23.3 s, natively landscape — **no `--rotate-cw`**), 117 frames @5 fps, windowed ws=24 kfi=6 overlap_kf=**0**, conf p55 | — | 29.8 s, 3.93 fps, peak VRAM 6.08 GB, 1.96M pts, 1 window, ratio 6.4, traj_step_max 7.8× median | single window achieved, but the cloud is visibly fragmented — torn, radially streaked sheets, no coherent room |
| 2026-08-05-landscape-inroom | 766cdf0-dirty | base, hallway entry/exit trimmed (`--start 3.5 --duration 16.5`), 83 frames @5 fps, windowed ws=24 **kfi=4**, conf **p70** | — | 17.9 s, 4.65 fps, 6.13 GB, 1.00M pts, 1 window, **ratio 2.87** | ratio landed in the "healthy" band while the render stayed bad — **`traj_length_over_extent` does not track visual quality** and must not be used as the only gate |
| 2026-08-05-landscape-6frame | 766cdf0-dirty | base, **`--first_k 6`**, streaming, conf p40, pixel_stride 1 | — | clean coherent surface: window, blinds, wall, floor all correctly placed | **the export math is correct.** Per-frame depth→world unprojection is sound; all degradation is pose accumulation across frames, not a bug in `reconstruct.py` |
| 2026-08-05-landscape-stream-10fps | 766cdf0-dirty | base, in-room segment @**10 fps** (165 frames), streaming kvsw=24 | — | 6.5 GB, traj_step_max **0.205** vs 0.732 at 5 fps | doubling temporal density roughly quartered the worst pose jump; 5 fps is too sparse for handheld panning despite the old sweep's conclusion |
| 2026-08-06-upstream-demo-loop | 766cdf0-dirty | **upstream `demo.py` verbatim**, `--image_folder example/loop --use_sdpa`, base ckpt | — | **FAILED** — `RuntimeError: CUDA driver error: device not ready` inside `load_model`'s `model.to(device)` | upstream's loader does `torch.load(map_location=device)` *then* `.to(device)` — two 4.63 GB allocations on an 8 GB card. **`demo.py` cannot load this model on this box at all**; `load_model_lowmem()` in `reconstruct.py` is load-bearing, not premature optimisation |
| 2026-08-06-upstream-loop-defaults | 766cdf0-dirty | our loader, **upstream inference defaults**: streaming, kfi=1, **kvsw=64**, nsf=8, on `example/loop` (237 fr) | — | **FAILED** — clean `torch.OutOfMemoryError` at the 6.80 GiB cap, no distro crash | upstream's default cache is 2.7× what this card allows; the project's downscaling to kvsw 24 was necessary, not a mistake. `--vram_fraction` turned a potential WSL kill into a catchable exception |
| 2026-08-06-upstream-loop | 766cdf0-dirty | our loader, `example/loop` (237 fr), streaming, **kfi=1**, kvsw=24, nsf=8, cami=4, conf p55, base ckpt | — | 77.7 s, 3.05 fps, peak VRAM **7.19 GB**, **4.07M pts**, 1 window, ratio 3.36 | **the reference baseline: a clean, coherent office loop on this exact GPU.** Proves the tool, checkpoint, wrapper and box are all fine — and that a *higher* ratio (3.36) can look far better than a lower one (2.47), killing that metric as a quality gate for good |
| 2026-08-06-landscape-kf1 | 766cdf0-dirty | our room, in-room 165 fr @10 fps, **long ckpt**, streaming, **kfi=1** (upstream default), kvsw=24, nsf=8 | — | 52.2 s, 7.10 GB, 2.73M pts, 1 window, ratio **2.47** — still visually unusable | the last untried upstream default changes nothing on our footage. **Motion analysis settles it:** our clip = 578° rotation / 5.36 translation = **107.9 °/unit**, vs `loop`'s 429°/36.02 = **11.9 °/unit**. Pivoting in place yields no parallax; config cannot manufacture it. Rule zero added to [[capture-protocol]] |
| 2026-08-05-landscape-long | 766cdf0-dirty | **`lingbot-map-long.pt`**, in-room 165 frames @10 fps, streaming, kfi=5, kvsw=24, **num_scale_frames=8**, **camera_num_iterations=8**, conf p60, pixel_stride 1 | — | 52.8 s, 3.12 fps, peak VRAM **6.98 GB**, **9.72M pts**, 1 window, **ratio 2.58** | **best reconstruction to date** — room shell readable (walls, corner, ceiling, framed art, blinds). Long checkpoint loaded into the same architecture with no missing keys. Surfaces still show "corduroy" ribbing from residual per-frame pose jitter |

| 2026-08-06-courthouse-kv16 | 766cdf0-dirty | `example/courthouse` (286 fr, outdoor), streaming kfi=1, **kvsw=16**, nsf=8, `--mask_sky`, base ckpt | — | 80.4 s, 6.30 GB, 5.26M pts, 114k sky pts masked, **ratio 24.88**, traj_step_max 7.9× median | first outdoor run. Per-frame geometry excellent (banners and window mullions legible) but **poses scribble**; kvsw dropped to 16 only because kvsw 24 OOM'd at frame 240/286 |
| 2026-08-06-courthouse-kf2 | 766cdf0-dirty | same, **kfi=2** (horizon 48 frames) + kvsw=24 | — | 81.6 s, 7.09 GB, 5.29M pts, **ratio 25.10** | doubling the memory horizon at constant VRAM changed nothing (24.88 → 25.10). **The pose failure is not a cache-size effect at the margin** — it needs upstream's whole 64-view cache, not a bit more |
| 2026-08-06-courthouse-sweep | 766cdf0-dirty | 6 variants chasing upstream's default horizon: kvsw 48/64 at `--stride 2`; `lingbot-map-long` at kvsw 24; windowed ws=48 overlap_kf=8 | — | **every one OOM** at the 6.80 GiB cap | **kvsw ≈ 24 is this box's hard ceiling, independent of sequence length** — halving to 143 frames did not buy kvsw 48. `lingbot-map-long` cannot run 286 frames at all. Upstream's published courthouse demo is simply unreachable on 8 GB; courthouse abandoned |
| 2026-08-06-loop-scale | 766cdf0-dirty | `calibrate_scale.py` on `upstream_loop`, camera anchor @ 1.5 m eye height | — | **2.4938 m/unit**; scene 18.1 × 11.2 × 26.7 m; camera 0.6016 units above floor (p5–p95 spread 37%) | first metric scale in the project. **Picking the ground plane by inlier count is wrong** — a corridor's walls outvote its carpet and the first fit gave camera heights spanning 0.33–6.22 units. Selecting the plane that keeps camera height *constant* fixes it |
| 2026-08-06-loop-clean | 766cdf0-dirty | `clean_cloud.py --scale auto`: statistical(20, 2.0) → radius(3 cm, 12) → voxel **2 cm** → align to calibrated plane | — | 4.07M → 964,011 pts (−3.2% statistical, −2.0% radius); extent 17.8 × 28.6 × 2.4 m; floor at z=0 | first cloud in true metres. Scale is applied *before* filtering so the skill's 2 cm voxel means 2 cm |
| 2026-08-06-loop-hfield | 766cdf0-dirty | `cloud_to_hfield.py --surface ground --crop --smooth 2`, 5 cm cells, p3 per cell, ground-tol 0.4 m, median 3×3 | — | 572×357 grid, relief **0.394 m** over 17.9 × 28.6 m, **9.0% observed**; drop test 6/6 clean, err ≤ 0.19 cm | **the skill's robust max-z is wrong indoors** — it makes every cubicle partition a 2.3 m spire and the hfield a canyon. `ground` mode (low percentile + reject cells >0.4 m above floor) gives a walkable surface. `top` stays the default for the outdoor trail this targets |
| 2026-08-06-loop-g1-settle | 766cdf0-dirty | `settle_g1_recon.py`, G1 from Menagerie, `crouch` keyframe, terrain solref 0.008 | — | pelvis **0.741 m**, |v|=0.001, drift 3.8 cm total / 0.1 cm post-settle, penetration 7.65 mm impact / **5.01 mm** steady (left ankle) | **Phase 4 closes: the G1 stands on terrain built from real video.** Three false alarms first — the gate counted the crouch pose's own thigh-vs-calf self-contact as terrain penetration, spawned the robot on a desk, then wedged it against a partition. Unsmoothed terrain topples it: ~5 cm of monocular depth noise on flat carpet is enough |
