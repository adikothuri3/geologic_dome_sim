# 2026-W32 (Aug 3–9)

Phase 1 — MuJoCo fluency. Planned Aug 3–9; landed **Aug 2**, a week early, but not on the
environment we planned to use.

## Sun Aug 2 — Phase 1 done, on Windows instead of WSL2

Demo: `phase1_stand.mp4` — G1 posed through stand → crouch → stand → t-pose → arms-up →
stand on a numpy-generated heightfield, orbiting camera, 471 frames / 18.8 s at 25 fps.
Contact sheet in `reports/phase1_contact_sheet.png`.

### What broke, in order

**1. `wsl --install` did nothing.** Looked like a hang. It wasn't — no `wsl`, `dism` or
installer process existed at all, `LxssManager` was never registered, nothing WSL-related
was staged for reboot. Root cause: `VirtualizationFirmwareEnabled = False`. **AMD SVM is
disabled in the BIOS** and Gigabyte ships B550 boards that way. No OS-side fix exists on
consumer Gigabyte boards — they expose no WMI BIOS provider the way Dell/Lenovo/HP business
machines do. Needs a physical trip to firmware: `shutdown /r /fw /t 0` → Tweaker → Advanced
CPU Settings → SVM Mode → Enabled.

Lesson: "stuck" is worth five minutes of process-list checking before waiting on it. Nothing
was running.

**2. Pivoted to native Windows.** Phase 1 turned out to need none of WSL: MuJoCo physics is
CPU, and Windows rendered through WGL with `MUJOCO_GL` unset on the first try — no EGL/OSMesa
fight at all, which was the risk I'd been most worried about. The whole environment was up in
about ten minutes.

**3. `git clone` failed TLS.** `unable to get local issuer certificate`. Already documented in
`notes/setup.md` from the skills install — global git config pins OpenSSL and fails against
GitHub on this box. `GIT_CONFIG_*` + `http.sslBackend=schannel` fixed it. The vault paid for
itself here; I'd have burned 20 minutes rediscovering that.

**4. `mediapy` needs a system ffmpeg.** Fine on Linux, absent on Windows. Swapped to
`imageio` + `imageio-ffmpeg`, which bundles its own binary — one code path for both OSes now.

**5. Duplicate keyframe name.** Declared my own `stand` key in the scene while `g1.xml`
already had one → `repeated name 'stand' in key`. Mine was a verbatim copy anyway, so I
deleted it and inherited Menagerie's. One fact, one home, even in XML.

**6. Meshes not found (the fiddly one).** `<include>`ing `g1.xml` from `sim/` broke every
mesh path. `g1.xml` declares `meshdir="assets"`, and MuJoCo resolves that relative to the
**top-level** scene file, not to the included file — Menagerie's own `scene.xml` never hits
this because it sits beside `g1.xml`. Two false starts before I stopped theorising and wrote
a probe script trying five configurations. Answer: our `<compiler>` must come **after** the
`<include>` (MJCF applies elements in order, so putting it first lets `g1.xml`'s own meshdir
win), with `meshdir` relative to `sim/`.

Also learned the error message names a *random* missing mesh each run — MuJoCo loads meshes
in parallel. I briefly read that as "progress" between attempts. It wasn't.

**7. Sphere drop test failed, and the test was wrong.** Three of four probes reported "still
moving." The spheres were **rolling downhill** — MuJoCo models sliding friction, not rolling
resistance, so a sphere never settles on a 4° slope. The one that passed was the flat spawn
pad, which should have been the tell. Switched the probe to a box. The `mjcf-terrain` skill
says "sphere/box"; for sloped terrain it has to be a box.

Worth noting the heights matched `sample_height` to within a centimetre even while failing —
that's the row/col orientation empirically confirmed, which is what Phase 4 actually needs.

**8. The robot fell — on the last transition only.** Held stand, crouch, stand, t-pose,
arms-up all at 0.0 cm drift, then toppled going arms-up → stand. Swinging both arms down
1.8 rad in one second dumps angular momentum into a floating base with straight legs and no
ankle strategy to absorb it. Not a terrain problem. Fixed with a slew-rate limit on the
position targets (`MAX_JOINT_RATE = 0.8 rad/s`, so that blend stretches to 2.25 s) plus
smoothstep easing so the rate is zero at both ends.

This is the most useful thing I learned all day: **position actuators hold joint angles, not
balance.** Phase 1 gets away with posing only because the spawn pad is level. Everything a
policy is for starts exactly here.

**9. Terrain looked flat on camera.** Demo passed all assertions, but 6 cm of relief over a
6 m field is invisible at a downward camera angle — and my own verification criterion said
"visibly non-flat terrain." Raised `elevation_z` to 0.15 m and dropped the camera to −8°
so relief reads in silhouette against the sky. Passing assertions ≠ a good demo.

### Experiment: `FLAT_PAD_FRAC = 0` — what the pad is actually holding up

Ran the demo with the level spawn pad disabled, to see the boundary rather than assume it.
Evidence: `reports/phase1_fall.png` (the topple), `reports/phase1_fall_poses.png` (after).

It fell in **under a second**, during the 1 s settle, before the pose sequence even started:

```
settled: pelvis 0.383 m above terrain        # with the pad: 0.792 m
stand  held 2.0s  clearance 0.081 m  drift 26.2 cm
```

Spawned on ~10° of slope, straight legs locked, CoM outside the support polygon. No recovery
mechanism exists, so there was no recovery.

**The result worth keeping** is what the log said *after* it fell:

```
crouch   held 1.5s  clearance 0.101 m  drift 0.0 cm
t_pose   held 1.5s  clearance 0.073 m  drift 0.0 cm
arms_up  held 1.5s  clearance 0.085 m  drift 0.0 cm
```

**0.0 cm drift — perfect joint tracking — while flat on its back.** All 29 actuators held
their commanded angles to a fraction of a degree the whole time. `kp=500` knows exactly one
thing: joint error. It does not know about gravity, base orientation, or contact. The robot
performed a crisp t-pose in the dirt and reported success.

Two consequences:

- **The `never collapsed` assertion is load-bearing, not decoration.** Judging this run by
  tracking error alone would have scored it a pass. Any Phase 5 eval harness needs a base
  height/orientation term that is *independent* of joint tracking, or a fallen policy looks
  good on the metrics that are easiest to log.
- **~10° of slope is not mild** for a straight-legged stand — I'd assumed it was. The pad is a
  bigger crutch than the slope statistics suggested.

This is now the baseline for Phase 2: a policy standing unaided with `FLAT_PAD_FRAC = 0` is a
real before/after, and the failing video exists to compare against.

### Open / next

- **BIOS SVM is now the critical path.** Phase 2 is MJX, which needs JAX-CUDA, which needs
  WSL2, which needs SVM. Must be done before Aug 16. `scripts/setup_wsl.sh` automates the
  entire Linux side once it boots.
- The level spawn pad (`FLAT_PAD_FRAC` in `terrain/make_hfield.py`) is a real crutch and is
  disclosed as one. A Phase 2 policy should stand on raw terrain with the pad set to 0 —
  that's a clean before/after measurement worth capturing.
- No `notes/experiments.md` row this week by design: no training or reconstruction run yet.
  First rows land in Phase 2/3.

## Mon Aug 3 — WSL2 environment up; five install attempts to get there

SVM enabled in firmware, reboot done, and the whole Linux toolchain is now live: Ubuntu 24.04,
hardware EGL, jax 0.11.0 on `CudaDevice(id=0)`, Playground 0.2.0, `G1JoystickFlatTerrain`
loading and stepping. **Phase 1 reproduces identically on Linux** — same drop test, same
assertions, same MP4 — which is the parity check worth having before Phase 2 starts.

It took five runs of `setup_wsl.sh`. Worth writing down, because only one of the five failures
was environmental.

### The install failures, in order

**1. PowerShell ate a `$` meant for bash.** `setup_wsl_stage2.ps1` built the user-creation
script in a double-quoted here-string containing `\$(id $UnixUser)` — backslash escapes in
bash, but PowerShell escapes with a backtick, so it evaluated `$(id aditya)` itself and tried
to run `id` as a cmdlet. With `$ErrorActionPreference = 'Stop'` the script died before the
user existed. Fixed with a single-quoted here-string (`@'...'@`, no interpolation at all) and
an explicit `__USER__` placeholder.

Related, and worse: PowerShell also mangles multi-word output coming back from `wsl.exe`
(`user ok: uid=1000(aditya)` arrived as just `user`). At one point that made a *failed* run
report `EXIT=0`. A false success is more expensive than a failure — everything now runs as a
script invoked by path, with the exit code captured inside bash where PowerShell can't reach it.

**2. `curl: (28) SSL connection timeout` on the uv installer.** Looked transient.

**3. Same timeout, different host** — `git clone` of Menagerie. Two hosts, two runs, so not
transient. Measured it: **~1 in 5 fresh HTTPS connections from WSL hangs until timeout, while
a sustained transfer runs fine at 6.3 MB/s** (507 MB pulled from codeload as proof). It's
connection setup that's flaky, not bandwidth.

Also caught myself here: my `retry()` wrapper hadn't fired, and my first instinct was that
retry was broken. It wasn't — the clone *succeeded*, and the failure was in the unwrapped
`git sparse-checkout set` on the next line, which triggers a lazy blob fetch on a
`--filter=blob:none` clone. That's a fresh connection, and it drew the unlucky one. **The
command that fails is the one that needs wrapping, not the one nearby that looks like it.**

Real fix was to delete the network call instead of retrying it: the Windows box already has a
sparse Menagerie clone that Phase 1 renders from, so the Linux side now copies `unitree_g1`
out of it. Zero network on the common path.

**4. `uv venv` refuses an existing venv.** Run 2 created it before dying at Menagerie, so run 3
tripped over it immediately. Every failure leaves partial state behind, so *every* step has to
be re-runnable — now guarded on `[[ -x "$VENV/bin/python" ]]`.

**5. The one-hour "hang" that wasn't mine.** `check_phase2.py` sat for over an hour, then died
with no log. Two things stacked: `registry.load()` clones Playground's **own** pinned Menagerie
(~500 MB, commit `1b86ece`) on first use, and that clone drew a stalled connection; meanwhile
WSL auto-terminated the idle distro, which killed the process and wiped `/tmp` — where I was
writing the log. `systemd=true` in `wsl.conf` mounts `/tmp` as tmpfs. Logs now go to `/mnt/c/`.

### The finding that actually matters for Phase 2

Chasing that hang surfaced something better than the fix:

```
Failed to allocate device memory of 6.00GiB: CUDA_ERROR_OUT_OF_MEMORY
  ... retried 5.40GiB, 4.86GiB, 4.37GiB
```

JAX preallocates 75 % of VRAM — 0.75 × 8188 MiB is *exactly* the 6.00 GiB that failed. It
fails because this GPU also drives the Windows desktop, which is holding ~2 GB. XLA backs off
until something fits, so nothing crashes; it just starts every run with a fragmented pool.

**The real training budget is ~6 GB, not 8.** Both scripts now set
`XLA_PYTHON_CLIENT_PREALLOCATE=false`. This is the kind of thing that would have looked like
"MJX is mysteriously slow" three weeks from now.

### DimOS: isolated on purpose, and it paid off immediately

`dimos[base,unitree]` resolves ~289 packages including torch and a second full CUDA stack,
plus its own numpy pin — into the same venv as MJX, as originally written. That trades a
working Phase 2 (due Aug 16) against a Phase 6 integration (due Sept 27). Moved it to
`~/venvs/dimos`.

First run in isolation failed on `pyaudio` needing `Python.h` and portaudio headers — and the
Phase 2 environment was untouched, exactly as intended. Added `python3.12-dev` +
`portaudio19-dev`.

### Open / next

- Phase 2 proper: `train_g1.py --smoke`, then the baseline run at `num_envs=2048`. If it OOMs,
  drop to 1024 before touching batch maths — `num_minibatches=32` is what keeps gradients
  identical to upstream.
- Playground's pinned Menagerie is cached now, but a fresh box pays that ~500 MB clone on the
  first `registry.load()` and it can look like a hang. Noted in `notes/setup.md`.
- Still no `experiments.md` row — the smoke run writes the first one.

## Tue Aug 4 — first two locomotion policies, and full-body collision

Two 2048-env runs landed. Videos: `reports/baseline_feetonly.mp4` and
`reports/full_collision_200M.mp4` (both walking forward at 1 m/s, 8–10 s, neither falls).

| Run | Collision | Steps | Peak | Final | Wall |
| --- | --- | --- | --- | --- | --- |
| `2026-08-04-g1-joystick` | feet-only | 100M | **4.53** @ 78M | 2.96 | 27 min |
| `2026-08-04-g1-joystick-full-collision` | full-body | 200M | **6.18** @ 84M | 1.58 | 59 min |

### The finding that matters: both peak near 80M, then decline

Not a plateau — a drift downward, over many consecutive evals. The feet-only run ended 1.6
below its own peak, the full-collision run **4.6** below.

And brax returns the *final* params. So both saved policies are worse than the best policy
the run actually found, and training longer made the saved artefact worse while the log looked
like it was merely flattening out. Doubling to 200M did not buy a better policy; it bought
more room to decay.

Fixed by checkpointing every evaluation via brax's `policy_params_fn`, plus a `best.json`
recording which step won. `render_policy.py --best` renders that one. Checkpoints are a few MB
and there are only `num_evals` of them — a trivial price for not discarding the best result of
an hour's compute.

Eval std is ±8–10 throughout, so any single eval is noisy; the *trend* is what's persuasive,
not any one number. Worth remembering before reading much into a single reward value.

### Full-body collision cost far less than expected

Collidable geoms went 1 → 24 (torso, pelvis, hips, thighs, shins, shoulders, arms, hands,
feet), each sized from the Menagerie mesh of the body it wraps.

Throughput cost: ~5–8 % (21M steps in 7.5 min vs 22.3M in 7.3 min). Learning curves are nearly
identical early on — at ~11M steps, −2.31 vs −2.38 — and full-collision reached a *higher*
peak. So the honest summary is that it costs almost nothing and, on this evidence, does not
make flat-ground walking harder.

What it actually buys is not reward, it is **correctness**: with feet-only collision the legs
can pass through each other and through the torso, so a policy is free to learn a gait that
cannot exist on the real robot. That is the worst class of sim2real gap — invisible in sim,
fatal on hardware. On Everest scree, shin and knee contact is normal rather than an error
state, which is the real reason to carry it.

### Two upstream faults the gate caught

`check_full_collision.py` refused to pass the first two attempts, both times correctly:

1. **`left_thigh` and `left_shin` interpenetrate by 16 mm at the nominal pose.** Harmless while
   nothing collides; the moment broadphase was on it became a separating force that toppled the
   robot before it took a step (`pelvis_z` 0.79 → 0.227 during the settle). Overlapping body
   pairs are now detected and excluded rather than guessed at.
2. **The shin capsules are radius 0.08** — a 16 cm-thick shin — reaching low enough to graze
   the floor while merely *standing*. That would have fed permanent phantom ground contact into
   `feet_slip`, `feet_air_time` and the contact sensors, corrupting the reward for reasons no
   log would have explained. Refitted to the mesh (9 cm), which also cut required exclusions
   from 6 to 2 and so *preserved* more genuine self-contact.

Both were invisible in a render and would have been near-impossible to diagnose from a reward
curve. This is the argument for asserting physical properties instead of eyeballing a video.

Side effect: `left_foot_right_shin_found` / `right_foot_left_shin_found` now work. Upstream's
`_get_termination()` reads both to catch leg crossing, but their `<pair>` entries are commented
out, so that half of the termination test could never fire in the feet-only baseline.

### Process notes

- **A run must not depend on the process that launched it.** The first 200M attempt was killed
  at 67M steps because the harness stopped the background task, and killing `wsl.exe` takes the
  whole WSL session with it. Relaunched under `setsid`; it then survived to completion.
- **A SIGKILLed run writes no `experiments.md` row** — Python's `finally` never runs. The row
  is the record, so the run has to survive to write it.
- **`/tmp` is tmpfs under `systemd=true`.** An idle-terminated distro took a log with it, and
  the evidence for a failure vanished with the failure. Logs now go to `/mnt/c/`.
- My progress monitor reported `PROCESS GONE without EXIT=` on a run that had actually
  succeeded — it checks for the exit marker, then for a live process, and the run exited in the
  window between. False alarm, but the ordering is worth fixing before it cries wolf again.
  **Update:** adding a 10 s grace period did not fix it — it misfired again on the 100M run,
  which had also succeeded. The log lives on the `/mnt/c` drvfs mount and the final write lands
  slower than that. Two false deaths, zero true ones: the monitor's death signal is not
  trustworthy and the log is the authority.

## Tue Aug 4 (later) — Phase 2 closed out: best-checkpoint policy + the stretch experiment

### The corrected premise

Spent part of this session about to "add" domain randomization and parallel envs. **Both were
already running in every policy trained so far.** `config.json` records `num_envs: 2048` and
`train_g1.py` passes `randomization_fn=registry.get_domain_randomizer(...)`, which is
`@jax.vmap`'d with `in_axes` mapping each randomized field to axis 0 — so all 2048 envs train
on *different physics simultaneously*, not one shared domain.

Worth the check before building: the roadmap's Phase 2 asks to **understand** what domain
randomization is doing, not to add it, and explicitly places terrain randomization, motor
strength and sensor latency in **Phase 5**. Written up in [[locomotion-policy]].

### The 100M run, with checkpointing

`best 4.74 @ 95M`, `final 2.65` — and this time the best was kept. It is genuinely better in
rollout, not just on the noisy eval metric: +0.0285 vs +0.0256 reward/step over a fixed-command
10 s clip, ~11 %.

It did **not** reproduce the 200M run's 6.18. brax splits its RNG per eval epoch, so a
100M/20-eval run is not the first half of a 200M/20-eval one — it is a different trajectory
that happened to land at 4.74. Both sit in the same 4.5–6.2 band. The 6.18 policy itself is
gone for good; it predates checkpointing.

### Stretch experiment: `feet_air_time` 2.0 → 4.0

Everything else held constant — same collision model, 2048 envs, 100M steps, seed 0.

**Total reward is not the comparison.** Changing a reward *weight* changes the scale the run is
scored on, so 3.05 vs 4.74 says nothing about which gait is better. `scripts/compare_gaits.py`
measures behaviour instead, under an identical command:

| Metric | baseline | `feet_air_time` ×2 |
| --- | --- | --- |
| swing fraction (share of time a foot is airborne) | 0.35 / 0.39 | **0.40 / 0.40** |
| steps per second per foot | 1.50 / 1.60 | 1.60 / 1.80 |
| forward velocity tracked (command 1.0 m/s) | **0.833** | 0.817 |
| velocity error | **0.167** | 0.183 |
| pelvis height | 0.750 ± 0.014 | 0.753 ± 0.016 |

The result is exactly what the term is meant to do, and it cost what you would expect:
**paying double for air time bought more air time** — swing fraction up, step rate up, gait
symmetrised (0.35/0.39 → 0.40/0.40) — **at the price of the thing you actually wanted**, with
velocity tracking degrading from 0.167 to 0.183 error. Reward terms compete; buying more of one
is paid for out of the others.

A separate observation from the same table: *both* policies track ~0.83 m/s against a 1.0 m/s
command, a consistent ~17 % undershoot. `tracking_lin_vel` is `exp(−err²/0.25)`, which is
generously flat near the optimum, so there is little gradient pressure to close that last bit.
Worth remembering before reading a Phase 5 velocity number as ground truth.

Videos: `reports/phase2_best_walk.mp4`, `phase2_best_turn.mp4` (same policy turning in place),
`phase2_stretch_airtime4.mp4`, `phase2_final_walk.mp4`.

## Tue Aug 4 (night) — Phase 3 toolchain up; LingBot-Map on an indoor clip

LingBot-Map installed from zero (`notes/setup.md` had no row for it), and `recon/` now goes
video → point cloud + camera trajectory → Open3D and viser. Ran it on `room_map.MOV`, a 2:12
handheld walkthrough of a bedroom.

**This does not close Phase 3.** The roadmap's demo is a *local outdoor trail* — rocky trail,
boulder field, steep stairs, rubble. A room is not that. But it was the right first clip
anyway: it doubled as the white-wall snow proxy, and it found a limit that would otherwise
have surfaced on Everest footage in October.

### The clip fought back before the model even saw it

Two properties of modern iPhone video that would have quietly degraded everything:

1. **Dolby Vision / HLG 10-bit HDR** (`transfer=arib-std-b67`, `bt2020nc`). Decoded naively
   to 8-bit sRGB the frames come out washed out and low-contrast — and contrast is precisely
   the signal a feed-forward depth model keys on. Needs a real tonemap
   (`zscale` → linear → `tonemap=hable` → BT.709), not a pixel-format cast.
2. **Portrait stored as landscape + a `displaymatrix: rotation of -90` side-data tag.**

Neither is visible if you just look at the video. `extract_frames.py` handles both and
*asserts* the resulting orientation rather than trusting ffmpeg's autorotation.

### Aspect ratio was worth 1.8 GB of VRAM

LingBot-Map fits **width** to 518 and only crops height. So:

- portrait 9:16 → `pad` mode → 518×518, of which **43% is white padding**
- the same frame rotated to landscape → `crop` mode → **518×294**, no padding, no cropping

Same field of view, 43% fewer tokens. Measured on an identical 60-frame clip: peak VRAM
**6.93 → 5.15 GB**, 3.17 → 4.07 fps, and *more* surviving points (409k → 451k). Rotating the
footage is strictly better than padding it. Added `--rotate-cw`.

(`pad` still matters for portrait input you can't rotate: upstream's hardcoded `mode="crop"`
would centre-crop 921 px of height down to 518, throwing away the near-field floor — the exact
thing Phase 4 turns into terrain. When padding is used, `reconstruct.py` detects the letterbox
border and masks it, or the depth head's predictions on white padding spray a fake plane into
the cloud.)

### Three ways this box says "no", none of them a normal OOM

1. **`OSError: [Errno 12] Cannot allocate memory`** loading ~1300 JPEGs — nothing to do with
   memory, it's drvfs. Frames have to be staged on ext4, not `/mnt/c`.
2. **OOM-killed at 7 GB RSS.** Predictions accumulate on the CPU in fp32 — images + depth +
   depth_conf ≈ 7.1 GB for 1320 frames, against 7.7 GB of WSL RAM. Worse, the run **reported
   exit code 0**, because `python … | grep …` returns grep's status. A false success is the
   expensive kind of failure; that's twice this week (the PowerShell one on Aug 3).
   `reconstruct.py` now preflights the RAM estimate and aborts in 5 s instead of dying 20 min in.
3. **`CUDA driver error: device not ready`, and once `Wsl/Service/E_UNEXPECTED`** — overshooting
   VRAM under WSL doesn't reliably raise a clean torch OOM; it can take the whole distro down
   (`wsl --shutdown` to recover). Setting `torch.cuda.set_per_process_memory_fraction` converts
   that into a catchable `torch.OutOfMemoryError`, which is how the `window_size 32` attempt
   failed afterwards — loudly and harmlessly.

Also: the 4.63 GB checkpoint doesn't fit the obvious load path. Upstream's
`torch.load(map_location=device)` puts it on the GPU *and then* allocates the model again on
`.to(device)`; loading with `mmap=True` and casting to bf16 before the transfer lands weights
at 2.81 GB.

### The finding that matters: ~25 seconds of footage per consistent scene

> [!warning] Corrected the next day — read the Aug 5 entry
> The version of this section written on the night of Aug 4 concluded "streaming mode is out,
> so windowed is mandatory, so stitching is the problem." The first clause was **my bug**, and
> it made the whole chain of reasoning wrong in an interesting way. Kept below as written,
> because the stitching evidence is real and the correction is the more useful lesson.

Streaming mode looked out — 660 frames pinned the card at 7858/8188 MiB and simply stopped
progressing (33/660 after ten minutes). So windowed mode seemed mandatory, which means windows
must be stitched.

They weren't. `inference_windowed` warps each window into the first window's frame using a
similarity transform estimated from overlapping keyframes, and on plain cream walls that
estimate is wrong:

| Windows | overlap keyframes | traj length | scene extent | ratio |
| --- | --- | --- | --- | --- |
| 13 | 4 | 88.6 | 2.60 | **34×** |
| 9 | 12 | 109.6 | 2.88 | **38×** |
| **1** | — | 6.75 | 2.45 | **2.8×** |

A camera path 34× the size of the room it walks around is not a camera path. There's a 2.14
pose jump at frame 164 against a median step of 0.071, right at a window boundary, and the
rendered trajectory is a scribble.

The instinct — "increase the overlap" — made it *worse*, which is the useful part. The failure
is **scale estimation on low-texture overlap**, not insufficient overlap. And that ties this
directly to the snow question: snow is the same texture regime, so this is not an indoor
curiosity. Per-frame geometry was fine throughout; what low texture cost us was *global
consistency*, which is a much easier failure to miss.

Deliverable is therefore the single-window run: 124 frames (25 s), 921k points, coherent room,
smooth trajectory, no pose jump. `run.json` now records `traj_length_over_extent` so this is
caught automatically next time — ≲3 healthy, tens means the stitch failed.

### Open / next

- **Phase 3 needs outdoor trail footage.** 2–3 min, slow and steady, locked exposure, plenty of
  overlap — and now, ideally, shot **landscape**, which is both cheaper and the orientation
  Pemba's onboard camera will produce anyway.
- Size clips to one window (124 frames at the current defaults) until stitching is solved, or
  accept per-window scenes. Phase 4 terrain should be built per-window for now.
- `lingbot-map-long` is not downloaded yet — that's the Phase 7 variant and it may behave
  differently on stitching. Worth testing on the trail clip.
- Scale is still arbitrary; every number above is in model units. Phase 4's marker calibration
  is the fix, and it should be filmed *into* the next clip rather than bolted on after.

## Wed Aug 5 — the streaming conclusion was wrong, and the real limit is ~25 seconds

Aditya asked the obvious question I should have asked myself: *doesn't Pemba run in streaming
mode?* The entire project premise is streaming reconstruction from a live camera, and I had just
written "streaming is not usable past ~100 frames" into five files. Those two things cannot both
be true.

They weren't. `recon/reconstruct.py` had:

```python
if a.mode == "streaming":
    images = images.to(device)          # ← all 660 frames, 1.2 GB, onto an 8 GB card
else:
    print("keeping inputs on CPU; windows move to GPU one at a time")
```

with a comment asserting streaming "has no such slicing, so it still needs the tensor resident."
`gct_stream.py` says the opposite in as many words: *"we slice-then-move per iteration so peak
GPU memory is O(scale) or O(1) frames rather than O(S)."* I had read that exact sentence in the
windowed model and generalised the wrong way. One line deleted; streaming then ran the entire
660-frame clip at a **flat 5.63 GB**.

**Lesson, and it is the same one as `retry()` on Aug 3:** when a measurement contradicts the
premise the whole project rests on, suspect the measurement. I had the contradiction in hand —
"streaming model can't stream" — and wrote it into the vault as a finding instead of a symptom.

### What the limit actually is

With streaming working, I could measure the thing that matters. Same footage, both modes,
`traj_length_over_extent` (camera path length ÷ scene size; ≲3 healthy):

| Clip length | streaming | windowed |
| --- | --- | --- |
| 25 s | **2.76** | **2.76** — byte-identical, 921,100 points |
| 49 s | 11.8 | 9.7 |
| 132 s | 34–46 | 31–38 |

They agree *exactly* at 25 s and fail together past it. So stitching was never the root cause —
it was one of two symptoms. The KV cache that keeps streaming memory bounded is also the model's
memory horizon (~24 keyframes ≈ 14 s here), and once the scene falls out of it, global
consistency goes: as scale jumps at window boundaries (windowed, per-window scale span measured
at **3.2×**) or as smooth drift (streaming).

Two other dead ends worth recording, both closed cleanly:

- **Sample the clip sparser to fit one window.** No: ≥5 fps is required. 0.2 s between frames
  gives 2.8, 0.3 s gives 8.6. The cliff is sharp, and it means you cannot trade frame rate for
  duration.
- **Drop the resolution to afford a bigger window.** No: `size mismatch for
  aggregator.patch_embed.pos_embed: [1,1370,1024] vs [1,577,1024]`. 518 is baked into the
  checkpoint.

### The consequence for the expedition

This upgrades the cloud-GPU question from throughput to **correctness**. A bigger card buys a
bigger KV cache, which buys a longer memory horizon, which is the only lever that extends the
25 s window. Two things to test before September: `lingbot-map-long` (built for this regime,
still not downloaded), and feeding Pemba's odometry as an external pose prior — the one signal
the expedition has that a handheld phone clip does not.

And the capture rule that follows is worth more than any of the tuning: **shoot for coverage,
not duration.** A deliberate 20–25 s sweep of a space reconstructs cleanly; two minutes of
wandering the same space does not, and no amount of subsampling rescues it.

## Aug 6 — courthouse abandoned, the whole chain closes on `loop`

Two goals today: get an *outdoor* reconstruction, and finish Phase 4 on something.

### Why upstream's courthouse demo doesn't reproduce here

Read `demo.py`'s defaults instead of guessing: `kv_cache_sliding_window=64`, and
`keyframe_interval` auto-selected to 1 for anything under the 320-view RoPE limit. That is
the config behind every published demo. Our courthouse runs used 16 and 24, because 64 OOMs.
So the published result isn't a different scene or a secret flag — it's the same config with
4× the memory horizon.

I spent six runs trying to buy that horizon back and failed at every angle: kvsw 64 and 48
both OOM even after halving the sequence with `--stride 2`, `lingbot-map-long` OOMs at 286
frames outright, and windowed mode — upstream's own documented remedy for pose collapse —
OOMs too. **kvsw ≈ 24 is a hard ceiling here and it is set by cache size, not sequence
length.** That last part surprised me; I'd assumed a shorter clip would free room for a
bigger cache. It doesn't.

The failure mode is worth describing precisely, because it's not what "bad reconstruction"
usually looks like. Per-frame geometry on courthouse is *excellent* — you can read the
banners on the building and count window mullions. What collapses is global pose: a 112-unit
camera path inside a 4.5-unit scene, `traj_length_over_extent` **24.9** against loop's 3.36,
and the facade stacked in visibly offset layers. Good depth, garbage poses. And it's a cliff,
not a gradient: doubling the horizon via `keyframe_interval 2` moved 24.88 → 25.10. Nothing.

Also built `--mask_sky` into `reconstruct.py` today. Upstream only wires sky segmentation
into its *viewer*, so our exported `.ply` never got it — on an overcast outdoor scene that
matters, since blown-out sky is a large, smooth, *confidently wrong* surface. Running skyseg
on the preprocessed frames rather than the originals sidesteps every crop/resize alignment
question. It removed 121k points. Kept, because the Everest footage will need it.

### Phase 4, and four false alarms

Pivoted to `loop` and took it all the way: scale → cleanup → hfield → drop test → G1. The
gates caught four bugs, and every one of them initially looked like a terrain defect:

1. **The ground plane was a wall.** Picking the RANSAC plane with the most inliers is wrong
   in a corridor — the walls carry more points than the carpet. Symptom: camera heights
   spanning 0.33–6.22 units, when a walking camera obviously holds a near-constant height.
   Fix: choose the plane that *minimises* camera-height spread. That's what "floor" means.
2. **The drop probe fell off the map.** My flat-cell search picked cells at the exact field
   boundary, so the box had half its footprint over the void. Reported as "tunnelled".
3. **The robot stood on a desk.** The flattest observed patch in an office is a desk top.
   It settled there very happily and the gate passed, having tested nothing.
4. **"Penetration" was the robot's own knee.** The gate measured max penetration over *all*
   contacts, so the crouch pose's thigh-against-calf overlap read as 5 mm of terrain
   penetration. Filtering to terrain contacts and attributing them by body name gave the real
   number: 0.3 mm on flat ground, ~5 mm where a foot corner loads a sloped cell — which is
   Menagerie's foot compliance, not our geometry.

The one *real* finding underneath all that: **robust max-z is wrong indoors.** The skill
prescribes it, and it's right for an outdoor trail where the top surface is what you walk on.
In an office it turns every cubicle partition into a 2.3 m serrated spire and the heightfield
becomes a canyon — the render of the G1 wedged between two of them is the clearest picture of
a correct rule applied outside its domain that I've produced on this project. Added
`--surface ground` (near-minimum per cell, reject cells >0.4 m above the floor) and left
`top` as the default, because the trail footage this actually targets wants `top`.

Second real finding: **monocular depth noise is load-bearing.** The reconstructed carpet
carries ~5 cm of roughness — consistent with the 4.5 cm floor-band RMS measured off the raw
cloud — and that is enough to topple a keyframe-posed G1 outright. `--smooth 2` is what makes
it stand. That number goes in the asset JSON, because it changes the terrain a policy sees,
and Phase 5 has to know it's there.

Final state: 4.07M points → 964k cleaned and metric at 2.4938 m/unit → a 572×357 hfield at
5 cm with 0.39 m relief → drop test 6/6 clean (≤0.19 cm) → G1 settling at pelvis 0.741 m,
0.1 cm post-settle drift. Phase 1's synthetic terrain still passes, so nothing regressed.

Honest limits on the artifact: only **9% of the field is observed ground**, the rest is
filled flat; `ground` mode deliberately drops all furniture and walls, so this is a walking
surface with no obstacles; and the terrain is an office corridor, which is not remotely the
rocky trail the roadmap's Phase 4 demo calls for. What's proven is the *plumbing*, end to
end, with gates that have now demonstrated they catch things.

## Aug 6 (later) — building the instrument for the courthouse question

Cleaned the repo out first: ~2 GB of dead reconstruction runs, the room-map artifacts I'd
scrapped, five empty OOM husks from the courthouse sweep, ~70 stale logs, and one tracked
validation report whose whole finding was "no terrain asset exists" — which stopped being true
on Aug 6 and had turned into an actively misleading file. Kept every training run and
`upstream_loop`, since the committed terrain assets came out of it.

Then wrote `colab/lingbot_map_colab.ipynb`, because the courthouse verdict I reached earlier
today is only *half* proved. What I actually established is that this box cannot run upstream's
config: kvsw 64 and 48 OOM, 24 OOMs at frame 240/286, only 16 completes, and `--stride 2` does
not buy a bigger cache. What I *inferred* — that a bigger card would therefore reconstruct
courthouse correctly — is untested, and inferring it is exactly the kind of move that produced
the "streaming doesn't work on 8 GB" claim I had to retract on Aug 5.

Design constraints I held to:

- **It shells out to `recon/*.py` unmodified.** A notebook that reimplements the pipeline would
  produce numbers that cannot be compared to the local ones, which is the entire deliverable.
  `reconstruct.py` already reads `LINGBOT_SRC` from the environment, so pointing it at a Colab
  clone took no code change at all.
- **`keyframe_interval` is pinned to 1, not left on auto.** This one nearly slipped past me.
  Upstream's auto is `(n_frames + 319) // 320`; ours is `ceil(n_frames / 240)`. They agree on
  loop's 237 frames and disagree on courthouse's 286 — ours gives 2. Running "upstream's config"
  with half upstream's keyframe density would have measured nothing and looked fine.
- **The sweep has a discriminator, not just a ladder.** `kvsw 16 / kfi 2` against
  `kvsw 32 / kfi 1` covers the same span of footage with half the cached views. That pair
  answers the question the ladder alone can't: is the model short of *views* or short of
  *horizon*? Views means VRAM is the whole lever and the cloud GPU settles Phase 7. Horizon
  means it doesn't, and a trek-length walk stays hard on any card.
- **The verdict is computed, not eyeballed.** The cell prints COMPUTE-BOUND / NOT-COMPUTE /
  PARTIAL against the ratio ≤ 6 bar that `calibrate_scale.py` already enforces before it will
  trust poses. I have now twice caught myself reading renders as evidence — and on Aug 5 logged
  a run that scored a healthy 2.87 while looking terrible — so the threshold goes in the code.

Not run yet; it needs an A100 or L4 session. Noted in the notebook that a T4 would drop
`reconstruct.py` to fp16 (bf16 is sm_80+ only), changing the numeric path at the same time as
the VRAM and muddying the one comparison the whole thing exists to make.

## Aug 6 (night) — the A100 answered, and the answer was "not the GPU"

Colab session on an **A100 80 GB**. Seven runs, ~20 minutes. The cache hypothesis I'd been
carrying since the morning is dead.

Courthouse across the whole `kv_cache_sliding_window` ladder: 16 → **24.84**, 24 → 24.76,
32 → 25.17, 64 → 25.48, 128 → 25.59. Eight times the cache, 3% change, and what change there was
went the wrong way. The discriminator I built the notebook around — `kvsw 16 / kfi 2` (25.19)
against `kvsw 32 / kfi 1` (25.17), same temporal span, half the cached views — came back null in
both branches. Controls held: kvsw 16 on the A100 is 24.84 against 24.88 locally, loop is 3.37
against 3.36. Same computation, different card, same answer.

So I'd inferred "a bigger card fixes courthouse" from "this card can't run their config", and it
was wrong. Exactly the shape of the streaming mistake I had to retract on Aug 5. The lesson isn't
subtle and I keep needing it: *cannot run X* does not imply *X would have helped*.

Aditya pushed back hard — same video, same config should mean same output — and he was right that
the premise deserved a real check rather than another hypothesis. Diffing our call against
`demo.py` came back clean: identical `GCTStream` kwargs, identical `inference_streaming` call,
identical dtype logic, and since the model returns no `world_points`, upstream's viewer unprojects
depth exactly like we do. Then he told me to read the paper, and the paper is where it was hiding.

**Upstream ships two pipelines and the README shows the weaker one.** `demo.py` is the quick-look.
The published videos come from `demo_render/batch_demo.py` via `process_videos.sh`:

    TARGET_FRAMES=4000  MODE="windowed"  WINDOW_SIZE=64
    FLOW_THRESHOLD=25.0  MAX_NON_KEYFRAME_GAP=100  IMAGE_STRIDE=1

Paper §4.4: VO mode for the large-scale demo videos, and adaptive keyframe selection that measures
optical flow against the most recent keyframe and promotes a frame only when it clears a threshold.
`demo.py` exposes that nowhere, and `gct_stream.py` doesn't implement it — it exists only in
`gct_stream_window.py`. Which means **every windowed run in this project has used fixed intervals**,
including the room-map stitching failures at 34–38×. That's our bug, not upstream's.

The number that closes it: upstream targets 25 px of flow *between keyframes*, with every
intermediate frame densely tracked. Consecutive frames in `example/courthouse` are already ~47 px
apart. Two frames from that folder, 100 and 101, swing across the whole corner of the building.
It is a decimated teaser, and nothing downstream recovers frames that were never in the folder.

Wired `--flow_threshold` / `--max_non_keyframe_gap` / `--conf_threshold` into `reconstruct.py`,
windowed-only and enforced. The model returns an `is_keyframe` mask, so the run record now carries
**`keyframe_frac`** — at 100% every frame cleared the threshold and the selector had nothing to
select. Smoke-tested locally: 38/48 keyframes on loop, so on dense footage it genuinely skips.

One thing nobody was looking for: the same loop scene calibrated to **2.4938 m/unit** locally and
**2.8343** on Colab. The camera-height anchor is not repeatable to better than 14% on identical
footage. `open-questions.md` had guessed ±15% and called it untested; it's now measured, and 14%
lands on every step height a Phase 5 policy would train on. That matters more than courthouse ever
did.

## Aug 6 (later) — EIG-1: the first footage that can tell me if I'm wrong

LingBot-Map has underperformed on everything I've fed it, and I finally admitted the common
factor is my footage, not the model. `room_map.MOV` is cream walls. `example/courthouse` is a
decimated teaser. `example/loop` is an office. None of them is a rocky outdoor trail, and — more
importantly — **none of them has a ground truth**, so every quality claim in this project has
been scored against `traj_length_over_extent`, a self-consistency proxy that already embarrassed
me once by scoring a terrible run at 2.87.

So: **GrandTour**, ETH Zurich RSL (arXiv 2602.18164). ANYmal-D with the Boxi payload, walked
across Switzerland. Mission **EIG-1** is the Eiger: 429 s, 219.7 m, rocky gravel path with
stairs and railings, and a survey-grade CPT7 GNSS/INS reference.

### Correcting the plan before running it

**arXiv doesn't host the video.** It hosts the paper. The data is on HuggingFace
(`leggedrobotics/grand_tour_dataset`) as zarr + JPEG tars, free and unauthenticated; the ROS
bags are behind a Google Form. This is better than what I was expecting — the JPEGs are already
the frame folder LingBot-Map wants, so there is no video decode step and no tonemapping guesswork.

**The camera in the paper's sensor table is not the camera in the release.** The table says the
TierIV HDR cameras are 1920x1280 @ **30 fps**. The released stream's own zarr attributes say
`"10Hz - 1920x1280 - TierIV C1 front"`, and the timestamp array has 4343 entries over 429 s.
30 fps is the sensor; 10 Hz is what shipped. Three image streams are actually available:

| stream | rate | resolution | distortion | shutter | frames |
| --- | --- | --- | --- | --- | --- |
| `hdr_front` | 10 Hz | 1920x1280 (3:2) | equidistant, 120x80 deg | rolling | 4343 |
| `alphasense_front_center` | 10 Hz | 1440x1080 (4:3) | equidistant, 126x92 deg | global | 4433 |
| **`zed2i_left_images`** | **14.91 Hz** | **1920x1080 (16:9)** | **radtan, k1=-0.065** | rolling | **6417** |

I went with **zed2i_left**, for three reasons that all point the same way:

- **48% more frames for the same walk.** Upstream's keyframe mechanism works by promoting a
  frame once its flow clears a threshold, with every intermediate frame densely tracked. More
  intermediate frames is exactly the input that mechanism wants.
- **Its distortion is nearly nothing.** `radtan` with k1 = -0.065 rectifies to pinhole with no
  meaningful crop. The fisheyes are 120–126 deg `equidistant`; rectifying those to a sane
  pinhole throws away 30 deg of real FOV. A feed-forward model trained on perspective imagery
  does not fail loudly on raw fisheye, it fails *quietly* — bent geometry, nothing flagged.
- **16:9 lands on 518x294**, which is the exact geometry every VRAM ceiling in `setup.md` was
  measured at. `hdr_front`'s 3:2 gives 518x350 — 19% more tokens per frame, for free, forever.

`recon/fetch_grandtour.py` does the whole thing: pulls the tars, decodes the zarr, rectifies to
an explicit pinhole **straight into the output raster** (one resampling, not two), and writes
`frames/`, `gt_tum.txt`, `mission.json`, and a contact sheet. The contact sheet exists because
the ZED2i is mounted upside-down on the payload — it turns out the release already corrects
that, but I was not going to assume it.

Two things that cost me time. The zarr chunks are **zero-padded to the declared chunk shape**,
which is enormous: `pose_pos` is chunked `[8388608, 3]` to hold 88801 rows, so decoding
everything in the ground-truth tar would have inflated `pose_cov` to 2.4 GB of zeros. And chunk
keys are dot-joined per dimension, so a 2-D array has no chunk file called `0`. Both are now
handled and commented.

The ground truth is a pose of `cpt7_imu`, and the camera sits **0.417 m** away on the payload.
That lever arm swings with every body rotation, which on a trotting quadruped is most of the
motion, so `fetch_grandtour.py` composes `T_enu_cpt7 . T_cpt7_base . T_base_cam` rather than
comparing against the IMU track. (`cpt7_imu` and `box_base` coincide exactly — the released tf
is identity — so it is really just the camera extrinsic.)

### What the mission actually looks like

From the CPT7 track: **212.6 m over 444 s, descending 26.6 m.** It is not one continuous walk.

```
   0- 40 s  0.2-0.4 m/s   starting up
  40-160 s  0.7-0.9 m/s   fast open descent on gravel  <- the clean section
 160-360 s  0.2-0.5 m/s   slow and technical           <- the stairs and railings
 380-400 s  0.00 m/s      stopped
 420-444 s  0.00 m/s      stopped
```

A 4x speed range with dead stops in it. **This is the strongest argument I have found for
`--flow_threshold` over any fixed `--keyframe_interval`**: no single interval is right for both
a 0.9 m/s descent and a full stop. A fixed interval spends keyframes on frames where the robot
is standing still and starves the fast section.

### The measurement that was supposed to decide keyframe_interval, and what it actually did

`recon/measure_flow.py` is the courthouse frame-spacing diagnostic promoted out of an ad-hoc
measurement into a script, because it is the number that decides whether a reconstruction can
work at all and I have now got it wrong twice by reasoning instead of measuring. Upstream
thresholds **mean dense flow magnitude at 25.0 px** (`process_videos.sh`); Farneback on the raw
frames measures the same physical quantity with no GPU, so it can gate a run before any VRAM is
spent. Full EIG-1, 518 px, 400 sampled pairs per interval:

| kfi | zed2i flow med | p90 | hdr flow med | s/keyframe (zed2i) |
| --- | --- | --- | --- | --- |
| 1 | **4.13** | 8.67 | 4.87 | 0.067 |
| 2 | 7.83 | 16.12 | 8.50 | 0.134 |
| 4 | 11.96 | 19.34 | 10.95 | 0.268 |
| 6 | 12.85 | 21.16 | 11.92 | 0.402 |
| 10 | 13.36 | 21.74 | 12.73 | 0.671 |

Everything is under 25 px, which at face value says "kfi 10 is fine, minimise your windows".
**I don't believe the top of that column.** The curve saturates around 12–13 px, and the two
cameras saturate at the *same* value despite running at different rates — kfi=4 on hdr is
0.396 s of real motion and on zed2i is 0.268 s, and they report the same flow. That is not
physics, that is Farneback decorrelating: past a few hundred milliseconds the gravel texture
stops matching and the estimator quietly returns something too small. The phase-correlation
column keeps climbing (2.3 -> 10.1 on hdr) where the dense one flattens, which is the tell.

So the honest read: **kfi=1 at ~4 px is solid, kfi<=4 is well-resolved and inside the regime,
and kfi >= 6 is unresolved by this proxy.** Which is fine, because the model will answer it
directly — see below.

### The lever I had backwards

The plan was to hold `--window_size 128` and sweep `--keyframe_interval` 4 -> 10 to cut window
count. The window arithmetic is right, and I confirmed it in
`gct_stream_window_v2.py:1047` — `window_size` counts **keyframes**, i.e. KV slots, and

```
actual_frames_per_window = num_scale_frames + (window_size - num_scale_frames) * keyframe_interval
```

so ws=128 / nsf=8 / kfi=4 really is 488 frames per window. But there are **two** ways to buy
window count, and they do not cost the same thing:

| lever | what it costs | on an A100 |
| --- | --- | --- |
| raise `keyframe_interval` | **keyframe spacing** — the thing that destroyed courthouse | not free |
| raise `window_size` | **VRAM only** — the geometry is untouched | ~free up to the RoPE ceiling |

Fitting the two windowed VRAM points I have measured at 518x294 (ws=16 -> 5.19 GB, ws=24 ->
6.21 GB) gives `VRAM ~= 3.15 + 0.128 * ws` GB. Against zed2i's 6417 frames, with
`--overlap_keyframes 8`:

| ws | kfi | frames/window | windows | s/window | VRAM |
| --- | --- | --- | --- | --- | --- |
| 24 | 1 | 24 | 401 | 1.6 | 6.2 GB (this box's ceiling) |
| 128 | 4 | 488 | **15** | 32.7 | 19.5 GB |
| 128 | 10 | 1208 | **6** | 81.0 | 19.5 GB |
| **256** | **2** | 504 | **14** | 33.8 | 35.8 GB |
| **256** | **4** | 1000 | **7** | 67.1 | 35.8 GB |

`ws=256 / kfi=2` buys the same 14 windows as `ws=128 / kfi=4` at **half** the keyframe spacing,
and `ws=256 / kfi=4` beats the kfi=10 window count while keeping keyframes 2.5x denser. Both fit
an 80 GB card with room. The ~320-view RoPE limit is the real ceiling on `window_size`, not VRAM.

Which also means the original sweep confounds two variables: changing kfi moves window count
*and* keyframe spacing together, so an ATE-vs-kfi curve cannot say which one caused what. The
experiment I actually want is two one-factor sweeps plus upstream's own config as the reference.

**And `lingbot-map-long` is free — but it is a genuinely different model.** `open-questions.md`
said it "is bigger still and cannot run 286 frames at all". The two checkpoints are both
4,632,303,465 bytes, 1342 tensors, 1.158 B parameters, same key set and same shapes — so
`-long` costs *exactly* the same VRAM, and whatever OOM'd that run was not model size.

I first wrote that up as "byte-identical in size and architecture", Aditya didn't buy it, and he
was right to check: that phrasing reads like the files are the same file. They are not.
SHA256s differ (`ee665103…` vs `832bc82c…`), and **1341 of the 1342 tensors differ numerically**
— `aggregator.global_blocks.16.attn.k_norm.weight` by mean |Δ| 0.549 against weights of
magnitude ~1.19, i.e. ~45% relative. Same architecture, separately trained. Both hashes match
`robbyant/lingbot-map` on HuggingFace, and **upstream lists both at exactly 4,632,303,465 bytes**,
so the equal size is a real property of the release and not a botched download.

Two things spotted while verifying: upstream also ships **`lingbot-map-stage1.pt`** (4,762,944,015
bytes — a genuinely different size, and we don't have it), and **`skyseg_batch.onnx`**, which is
the model `demo_render/config/outdoor_drive.yaml` actually names (`sky_batch_size: 64`) where we
use `skyseg.onnx` from `JianyuanWang/skyseg`.

I nearly wired the batch model into the Colab notebook on the assumption that ours "isn't
literally upstream's". Checked it instead, having just been caught doing exactly that with the
checkpoints: the two ONNX files are **the same network**. Identical output node names
(1959–1965), identical 320×320 geometry, and **bit-identical outputs on all seven heads** at
batch = 1. They differ by 40 bytes of graph metadata that makes the batch axis dynamic —
`skyseg_batch.onnx` accepts batch 4, `skyseg.onnx` rejects it. That is a throughput option our
per-frame call path doesn't use, so there was nothing to gain and a 176 MB download to lose. Our
sky masking *is* upstream's. Fetch removed, the measurement written into the cell so nobody
re-litigates it.

That is twice in one evening that "these files are different, so the behaviour must differ" was
worth ten minutes of checking rather than a paragraph of reasoning.

### It works, and the failure mode is visible

Local end-to-end run to prove the chain before renting anything: 25 s of the fast section
(60–85 s, 373 frames, 23.0 m of GT path), `lingbot-map-long`, windowed, `ws=24` (this box's
ceiling), `--flow_threshold 25.0`, `--conf_threshold 1.3`, `--mask_sky`.

```
keyframes selected: 97/373 (26%)
windows stitched: 5, per-window scale min 0.659 max 1.413 (span 2.1x)
traj_length_over_extent 1.27      <- courthouse was 24.9, room_map 34
inference 87.6 s, 4.26 fps, peak VRAM 6.02 GB
```

**`keyframe_frac` 26%.** This is the first time in the project the adaptive selector has had
footage dense enough to actually select — on courthouse it was 100%, meaning it had nothing to
skip. 26% is an effective keyframe interval of **3.8**, computed by the model on its own
predicted geometry, using upstream's exact metric. That is a far better answer than my Farneback
proxy could give, and it lands on **kfi ≈ 4** — which is where the plan started. The proxy and
the model agree at the bottom of the range and the model settles the top.

And `traj_length_over_extent` **1.27** is the healthiest number this project has produced on any
footage. LingBot-Map is not underperforming on EIG-1. It was underperforming on my footage.

Then the ground truth, which is the whole point:

```
ATE (Sim(3))     RMSE 1.168 m over 23.0 m of path   (5.07%)
recovered scale  5.4436 m per reconstruction unit
RPE over 5 m     median 0.645 m -> 12.9% drift rate
per-segment      0.87 / 0.47 / 0.83 / 1.22 / 0.83 / 1.48 / 1.42 / 1.70 m
```

The per-segment column climbs monotonically, and the error-vs-distance plot
(`runs/eig1-local25/recon-flow/ate_plot.png`) is a **sawtooth with four humps** at roughly 4,
11, 17.5 and 22 m. Five windows over 23 m puts a boundary every ~4.6 m. The humps are the window
boundaries. Per-window scale span is **2.1x**, i.e. the Sim(3) fusion is estimating scales that
disagree by a factor of two.

So the paper's warning — VO mode "incurs extra alignment error that compounds with the number of
windows" — is not theoretical here, it is the dominant error term, and I can see each boundary
in the plot. Which makes "minimise window count" the right instinct and `window_size` the right
lever, and turns the sweep into a measurement of something I have now actually observed instead
of a fishing trip.

The other prize: **5.4436 m/unit is the project's first scale anchored to a survey-grade
reference.** `calibrate_scale.py`'s camera-height anchor was found this morning to be repeatable
only to 14% on identical footage, with nothing to say whether either value was right. Now there
is something. `eval_ate.py` prints the two side by side whenever a `scale.json` exists.

### What I'd report to Pablo

Not "ATE vs keyframe_interval". Two one-factor sweeps against the CPT7 GT on the full 6417-frame
mission, plus upstream's own config as the reference point:

- **A — `window_size` at fixed keyframe density** (64 / 128 / 256, kfi=1): isolates the Sim(3)
  fusion cost, because keyframe spacing is held constant and only window count moves. This is
  the sweep that tests the paper's claim directly, and nothing in this project has done it.
- **B — `keyframe_interval` at fixed `window_size`** (1 / 2 / 4 / 6 / 8 / 10, ws=128): isolates
  keyframe spacing. This is the original plan, now with A to disentangle it from.
- **C — `--flow_threshold 25.0`, upstream's actual demo config**: the reference, and the run
  whose `keyframe_frac` says what interval the footage deserves.

Reporting ATE, RPE-over-10 m, `window_scale_span`, `keyframe_frac` and recovered scale for each,
against the CPT7 GT that all of them share.

None of this runs here. 6417 frames needs 19.5 GB of CPU RAM for the fp32 prediction stack alone
(this box has 7.7 GB and the preflight aborts in seconds), and `ws=128` needs ~19.5 GB of VRAM
against 8 GB. It is a Colab A100 job, which is what `colab/lingbot_map_colab.ipynb` already
exists to do — and this time the reason for renting is a hard resource wall, not a hypothesis
about one, which is the mistake I made this morning.

### Wiring it into the notebook

`colab/lingbot_map_colab.ipynb` gained a **Part 2** (45 cells now): fetch the mission, the flow
preflight, sweeps A/B/C, `eval_ate.py` scoring on every run, a results table, the figures, an
interactive plotly view of the winning cloud, and pre-formatted `experiments.md` rows. The
sweep defaults to the 40–160 s fast section (~1790 frames, ~5 min/run) rather than the whole
mission; `EIG_END = None` opts into all 6417 frames at ~25 min/run.

I ran the plotting cells against the local run before shipping them, which was worth doing —
the money chart referenced `r["windows"]`, a key that only exists on the *DataFrame*, not on the
merged run record. It would have crashed nine runs deep into a paid A100 session. Two more from
looking at the rendered PNGs rather than the code: both flow series were direct-labeled "dense
flow" (a label that names two different lines is not a label), and the scale-span panel had no
legend because the legend was attached to the other axes. Also found by reading rather than
running: the sky-model download is gated on `SCENES`, and EIG-1 is deliberately *not* in
`SCENES`, so a Part-2-only run would have silently skipped `--mask_sky` and put alpine sky at
effectively infinite depth straight into the cloud.

**SNOW-2** (`2024-11-03-07-57-34`, 261 s / 173.9 m) is the same script with a different
`--mission`, and is where I expect this to fall apart — low texture is the regime that broke
cross-window alignment on the room map. Worth flagging honestly: raising `--overlap_keyframes`
to 16 is the obvious move, but on Aug 4 tripling the overlap on `room_map` did **not** fix
stitching, because the failure was scale estimation on low-texture overlap rather than overlap
size. I expect it to fail again for the same reason. That is still the interesting result.

---

## Fri Aug 7 — the pivot: Isaac Sim replaces MuJoCo as the primary simulator

The ground-truth onboarding doc had us in MuJoCo through Phase 3+. Two things ended that.

First, the Robot Everest research deck spells out the team's actual stack, and it isn't
MuJoCo: *"Mapping the full Everest route using Lingbot-Map into IsaacSim, with domain
randomization over snow, ice friction, and wind gust … dedicated RL locomotion policies on
the route twin"*, plus GR00T/VLAs for task-level autonomy. MuJoCo was onboarding scaffolding.
If this onboarding is supposed to mirror the real deal, the sim has to be Isaac.

Second, LingBot-Map keeps being a headache on this box — the kvsw ≈ 24 ceiling, the ~25 s
drift horizon, courthouse unreachable even on an A100. So its role got demoted at the same
time: terrain comes from LingBot-Map **if it works**, otherwise stock/procedural mountain
terrain in Isaac (`TerrainGeneratorCfg` makes that fallback first-class). Reconstruction is
no longer on the critical path.

What actually happened today, in four commits on `isaac-pivot`:

1. **Deleted the vendored third-party mujoco skill** (`.agents/skills/mujoco`, 557 lines of
   robot-arm workflows nothing referenced) plus `skills-lock.json` and the
   `.claude/skills/mujoco` junction that made every WSL-side tree look dirty.
2. **Restructured the repo**: `sims/isaac/` (primary) + `sims/mujoco/` (working legacy) +
   sim-agnostic `recon/` feeding both. Every fragile import fixed (`parents[3]` anchors,
   CWD-dependent sibling imports, `cloud_to_hfield.py`'s relative output dir), the menagerie
   junction moved to `sims/mujoco/xmls/menagerie`, `phase1_stand.mp4` finally writes to
   `reports/` instead of the repo root, the obsolete 287 MB Windows `.venv` deleted, and the
   Colab blob re-baked (9 scripts now — terrain conversion is per-sim, not recon).
   All four legacy gates re-run in WSL and pass with pre-move numbers (settle: pelvis
   0.741 m, 5.01 mm steady-state penetration).
3. **Isaac scaffolding**: `sims/isaac/setup_isaac.ps1` (Isaac Sim 5.1.0 + Isaac Lab v2.3.2,
   Python 3.11, native Windows — scripted, deliberately not executed yet) and the README with
   the smoke ladder. Sobering spec check: this box is below Isaac's official minimum on
   *both* axes (8 GB VRAM vs 16, 16 GB RAM vs 32), so local = headless smoke tests at 64–256
   envs, real training = cloud GPU. That's the documented community pattern, not a hack.
4. **Docs rewritten for the pivot**: CLAUDE.md one-liner, overview mission + milestone table
   (7 phases kept; 4–7 re-targeted to Isaac), pipeline stages 4–7 now Isaac-native with
   MuJoCo demoted to a legacy subsection + permanent stage-7 sim2sim validator, a superseding
   decisions entry (the 2026-08-01 "rejected Isaac-family simulators" call is formally
   reversed — the reasons were real, matching the team's stack outweighs them), glossary
   gained the Isaac vocabulary, and the split-table formatting bug in experiments.md is fixed.

Nothing about Phases 1–2 is disowned: the MuJoCo track stays runnable, and its trained
policy becomes the gait sanity baseline the first Isaac policy gets compared against.

Next: run `setup_isaac.ps1`, climb the smoke ladder, and find out what a 16 GB-RAM machine
thinks of a simulator that wants 32.

## Fri Aug 7 (later) — Isaac Sim is alive on this machine

Installed and smoke-tested the whole Isaac stack in one sitting: isaacsim 5.1.0.0 +
Isaac Lab v2.3.2 (isaaclab 0.54.2) + rsl-rl-lib 3.1.2 on Python 3.11.9, native Windows,
torch 2.7.0+cu128 with CUDA confirmed. Five official NVIDIA Claude skills from
`isaac-sim/isaacsim` installed as real copies (no junction hazard this time).

Five failures on the way, each now baked into `setup_isaac.ps1` or the scripts:

1. **Norton Antivirus MITMs TLS.** Every Python/OpenSSL HTTPS call died with
   `CERTIFICATE_VERIFY_FAILED` while curl.exe/git-schannel sailed through — Windows trusts
   Norton's injected root, OpenSSL's bundle doesn't. Exported the Norton root from the
   Windows cert store and built `~\venvs\ca-bundle-norton.pem` (certifi + Norton);
   `PIP_CERT`/`SSL_CERT_FILE` point at it, git gets `-c http.sslBackend=schannel`. This is
   the same disease the WSL-era `http.sslBackend=schannel` note in setup.md was treating.
2. **`isaaclab.bat --install` can't see a plain venv** — it only detects conda or its
   bundled kit-python, and `flatdict` (an isaaclab dep) imports `pkg_resources` at build
   time, which pip's isolated modern setuptools no longer ships. Fix: direct
   `pip install -e source/...` of the four packages + `--no-build-isolation` for flatdict.
3. **The Omniverse EULA prompt** blocks headless first launch with
   `EOF when reading a line`. `OMNI_KIT_ACCEPT_EULA=YES`, persisted user-level.
4. **tensordict ABI crash**: rsl-rl pulls tensordict unpinned; 0.13.0's C extension is
   built against a newer torch and access-violates on import under torch 2.7.0. Pinned
   0.8.3.
5. **Kit hijacks python stdout and can crash on teardown.** Gate verdicts vanished into
   the void, and `env.close()` took the process down with a native access violation
   *after* training had succeeded — which ate the experiments row. Verdicts now go to
   stderr + `runs/isaac/gates.log`, and `train_g1_flat.py` writes its row before touching
   any teardown.

The payoff, all on the "below minimum spec" 8 GB / 16 GB box:

- Gate A: headless SimulationApp up in **7.7 s**.
- Gate B: **`Dome-G1FullCollision-Flat-v0`** — our own task; stock Isaac's G1 velocity
  task uses `G1_MINIMAL_CFG` with collision meshes stripped, the same feet-only shortcut
  Playground took — builds in 26 s, action_dim 37 on cuda:0, 50 zero-action steps with
  finite observations. PhysX visibly cooking the full set of G1 convex hulls in the log.
- Gate C: 10 RSL-RL PPO iterations at 64 envs, **~690 steps/s**, model_9.pt saved under
  `runs/isaac/`, experiments row written automatically.

So "below minimum" turns out to mean "don't open the GUI": headless physics at small env
counts is comfortable. The real Phase 4a run (1500 iterations) still belongs on a cloud
card. Next: that run, then Phase 4b terrain — LingBot mesh via MeshConverter if it
behaves, procedural mountain terrain if it doesn't.

## Fri Aug 7 (night) — the A100 sweeps came home, and they close the book on full-mission recon

`lingbotmap_colab_2026-08-08.zip`: sweeps A, B and C from the "What I'd report to Pablo"
plan, run on the **full** EIG-1 mission — 6417 frames, 429 s, 208 m of CPT7 ground truth.
Ten configurations. One result:

| lever | range | windows | ATE RMSE |
| --- | --- | --- | --- |
| A: `window_size` | 64 / 128 / 256 | 115 / 54 / 26 | 22.9 / 22.9 / 22.8 m |
| B: `keyframe_interval` | 1 → 10 | 54 → 6 | 22.4–22.9 m, flat |
| C: `--flow_threshold 25` | paper's adaptive | 8 | 22.9 m |

**Everything lands on ~23 m (11% of path), within half a meter of each other.** The top-down
plot says it plainly: CPT7 walks a 60 m descent; the reconstruction is a ~10 m scribble.

The subtle finding is *why* the levers don't work. I expected window-chaining scale decay to
be the villain — and A_ws128 does show a per-window scale span of **1144×**. But kfi=10 cuts
that span to **8.6×** (six windows) and the ATE does not move at all. So the scale wander is
a symptom of the same underlying long-horizon inconsistency, not the mechanism — and if
window count, keyframe density, *and* upstream's own adaptive policy all land on the same
number, there is no inference-time configuration left to try. (Caveat on record: all ten runs
used the base checkpoint; `-long` at mission scale is the one untested lever.)

What survives: the 25 s segment at 1.17 m / 5.1% is untouched by this — same footage, same
model, same GT. The tool is a **per-segment instrument**. The runbook's "≤10 min chunks"
was off by an order of magnitude; the working number is ~25–30 s per segment, stitched by
external odometry (Pemba's, on the expedition) if a route map is ever needed.

For the pipeline this is oddly clean: Phase 4b's terrain decision no longer hinges on hope.
Per-segment recon terrain is real (that's the loop-office → hfield chain, and next the
segment → OBJ → USD path in Isaac); full-route twins come from procedural terrain or
external mapping, not from LingBot-Map.
